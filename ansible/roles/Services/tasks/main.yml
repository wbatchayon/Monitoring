---
# Infrastructure Services Portal - Central monitoring access point

- name: Update apt cache
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600
  become: true

- name: Install nginx
  ansible.builtin.apt:
    name: nginx
    state: present
  become: true
